<?xml version="1.0" encoding="utf-8"?>
<MotionScene 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:motion="http://schemas.android.com/apk/res-auto">

    <Transition
        motion:constraintSetEnd="@layout/app_bar_main_end"
        motion:constraintSetStart="@layout/app_bar_main_start"
        motion:duration="300">
       <KeyFrameSet>
       </KeyFrameSet>

        <OnSwipe motion:touchRegionId="@+id/nav_drawer"
            motion:dragDirection="dragRight"/>
    </Transition>

    <ConstraintSet android:id="@+id/start">
    </ConstraintSet>

    <ConstraintSet android:id="@+id/end">
    </ConstraintSet>
</MotionScene>